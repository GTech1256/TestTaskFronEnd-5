// eslint-disable-next-line import/no-extraneous-dependencies
import { writable } from 'svelte/store';

/**
 * open or close popup
 */
export const basket = writable(false);
export const result = writable(false);
